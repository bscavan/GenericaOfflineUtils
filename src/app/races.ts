import { RacialJob } from "./racial-job";
import { Attributes } from "./attribute-keys";

export class Races {
	public getPeskieRace(): RacialJob {
		let attributesSet = new Set<Attributes>();
		attributesSet.add()
		let peskie = new RacialJob("Peskie" );
	}
}